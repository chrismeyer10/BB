package com.example.bahnandbike.feature.search.ui

// Holds the location shown on the map and the reason why it is used.
data class MapLocationUiState(
    val latitude: Double,
    val longitude: Double,
    val label: String
)
